name: Deploy UI to Render

on:
  push:
    tags:
      - 'prod-ui-*'

jobs:
  call-deploy:
    uses: ./.github/workflows/deploy-by-render-service-id.yml
    with:
      service_id: ${{ secrets.RENDER_CC_UI_SERVICE_ID }}
      commit_sha: ${{ github.sha }}