name: Deploy API to Render

on:
  push:
    tags:
      - 'prod-api-*'

jobs:
  call-deploy:
    uses: ./.github/workflows/deploy-by-render-service-id.yml
    with:
      commit_sha: ${{ github.sha }}
    secrets:
      RENDER_API_KEY: ${{ secrets.RENDER_API_KEY }}
      RENDER_SERVICE_ID: ${{ secrets.RENDER_CC_API_SERVICE_ID }}
