name: Deploy API to Render

on:
  push:
    tags:
      - 'prod-api-*'

jobs:
  call-deploy:
    uses: ./.github/workflows/deploy-by-render-service-id.yml
    with:
      service_id: ${{ secrets.RENDER_CC_API_SERVICE_ID }}
      commit_sha: ${{ github.sha }}
